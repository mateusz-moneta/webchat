# Webchat

Usage of technologies
- Angular 7
- Node.js (express.js)
- Socket.IO
- MySQL

Create database `webchat` and import tables from file `database.sql`

Run `npm start`. Navigate to `http://localhost:3000/`.
